<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% trans %}email.application.reminder.subject{% endtrans %}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #e67e22;
            color: white;
            padding: 15px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            padding: 20px;
            border: 1px solid #ddd;
            border-top: none;
            border-radius: 0 0 5px 5px;
        }
        .button {
            display: inline-block;
            background-color: #e67e22;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 4px;
            margin: 20px 0;
            font-weight: bold;
        }
        .footer {
            margin-top: 20px;
            font-size: 12px;
            color: #777;
            text-align: center;
        }
        .urgent {
            background-color: #fff3e0;
            border-left: 4px solid #e67e22;
            padding: 10px 15px;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>{% trans %}email.application.reminder.title{% endtrans %}</h1>
    </div>
    <div class="content">
        <p>{% trans %}email.application.reminder.greeting{% endtrans %} {{ user.firstName }},</p>
        
        <p>{% trans %}email.application.reminder.intro{% endtrans %}</p>
        
        <div class="urgent">
            <p><strong>{% trans %}email.application.reminder.reference_number{% endtrans %}</strong>: {{ submission.identifier }}</p>
            
            {% if deadlineDate %}
            <p><strong>{% trans %}email.application.reminder.deadline{% endtrans %}</strong>: {{ deadlineDate|date('F j, Y') }}</p>
            {% endif %}
        </div>
        
        <p>{% trans %}email.application.reminder.missing_items{% endtrans %}</p>
        
        <p style="text-align: center;">
            <a href="{{ continueUrl }}" class="button">{% trans %}email.application.reminder.complete_button{% endtrans %}</a>
        </p>
        
        <p>{% trans %}email.application.reminder.help_offered{% endtrans %}</p>
    </div>
    <div class="footer">
        <p>{% trans %}email.footer.no_reply{% endtrans %}</p>
        <p>© {{ "now"|date("Y") }} FabLab Platform. {% trans %}email.footer.all_rights{% endtrans %}</p>
    </div>
</body>
</html>